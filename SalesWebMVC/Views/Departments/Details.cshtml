@model SalesWebMVC.Models.Department

@{
    ViewData["Title"] = "Detalhes";
}

<h2>@ViewData["Title"]</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div class="container">
    <a asp-action="Edit" asp-route-id="@Model.Id"><button class="btn btn-info">Editar</button></a>
    <a asp-action="Index"><button class="btn btn-warning">Voltar</button></a>
</div>
